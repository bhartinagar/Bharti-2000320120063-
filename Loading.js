//import React, { useState } from 'react'

/*const Loading = () => {

    const [formValues] = useState([{ name: ""}])

    return (
        <form  onSubmit={handleSubmit}>
          {
          formValues.map((element, index) => (
            <div className="form-inline" key={index}>
              <label>Number</label>
              <input type="text" name="number" value={element.name || ""} onChange={e => handleChange(index, e)} />
            ))}
        </form>
    )
};
export default Loading;*/